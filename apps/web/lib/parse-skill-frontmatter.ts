// Re-export from the shared package -- the canonical implementation lives in
// packages/core/src/skills-frontmatter.ts.
export {
  parseSkillContent,
  reconstructSkillContent,
  type ParseResult,
} from "@tambo-ai-cloud/core";
