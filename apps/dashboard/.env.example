# Add real values and remove `.example` from the filename
# This is the dashboard app (app.tambo.co)

## Supabase
# Local Development: run `supabase start` (should match defaults below)
# Production: get from Supabase dashboard
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

## API Security
# Local Development: You can use the defaults below
# Production/External Facing: Should be a long random string (e.g. 128 characters)
# Used for encrypting and decrypting a projects' provider keys (e.g. OpenAI), so we don't store users' raw provider keys. Provider keys added with this secret can only be decrypted by this secret. You can use your own value here if running locally, but provider keys generated from the public API will not work with your local API, and vice versa.
PROVIDER_KEY_SECRET=API_KEY_SECRET_FOR_LOCAL_DEVELOPMENT_ONLY
# Used for encrypting and decrypting Hydra API keys. API keys generated with this secret can only be decrypted by this secret. You can use your own value here if running locally, but API keys generated from the public API will not work with your local API, and vice versa.
API_KEY_SECRET=API_KEY_SECRET_FOR_LOCAL_DEVELOPMENT_ONLY

## Slack Configuration (optional)
SLACK_OAUTH_TOKEN=
INTERNAL_SLACK_USER_ID=

## Email Configuration
RESEND_API_KEY=
RESEND_AUDIENCE_ID=
DISALLOWED_EMAIL_DOMAINS=gmail.com,yahoo.com,hotmail.com,outlook.com,aol.com,icloud.com,protonmail.com,zoho.com,yandex.com,fastmail.com,gmx.com,mail.com,tutanota.com,hey.com,inbox.com,mail.ru
# When self-hosting, you can restrict logins to a single verified email domain.
# If set (e.g. "foo.com") only users with verified emails ending in *@foo.com
# will be able to sign in. Leave unset to allow any verified domain.
ALLOWED_LOGIN_DOMAIN=

## PostHog Configuration (optional - analytics)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=

## Tambo API Setup
NEXT_PUBLIC_TAMBO_API_URL=http://localhost:8261
# Get this after you setup your local environment
NEXT_PUBLIC_TAMBO_API_KEY=
NEXT_PUBLIC_TAMBO_DASH_KEY=

## Smoketest Setup (optional)
NEXT_PUBLIC_SMOKETEST_TAMBO_API_KEY=
NEXT_PUBLIC_SMOKETEST_PROJECT_ID=

## Weather API Key (optional - for smoketesting)
# Go here: https://www.weatherapi.com/
WEATHER_API_KEY=

## Allow Local MCP Servers
# Set to true if you want to allow local MCP servers
ALLOW_LOCAL_MCP_SERVERS=false

## Github (optional)
GITHUB_TOKEN=

## NextAuth Configuration
# Run `openssl rand -hex 8` to generate a random secret
NEXTAUTH_SECRET=...
NEXTAUTH_URL=http://localhost:8260
# Get these from a developer. You must use the "developer" clients here, not
# "production" clients, so that they support localhost-based redirects
GITHUB_CLIENT_ID=...
GITHUB_CLIENT_SECRET=...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...

## Docs URL (for redirects)
NEXT_PUBLIC_DOCS_URL=https://docs.tambo.co

## Whitelabeling (optional)
NEXT_PUBLIC_TAMBO_WHITELABEL_ORG_NAME=
NEXT_PUBLIC_TAMBO_WHITELABEL_ORG_LOGO=

## Sentry configuration (optional - error tracking)
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ORG=
NEXT_PUBLIC_SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
