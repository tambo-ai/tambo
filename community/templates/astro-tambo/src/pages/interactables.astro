---
/**
 * Interactables Page
 * 
 * This page displays the interactive elements of the application.
 * It utilizes the `TamboInteractable` component which is rendered strictly on the client side
 * via `client:only="react"` to handle user interactions dynamically.
 */
import Layout from "@/layouts/Layout.astro";
import TamboInteractable from "@/components/interactables/TamboInteractable"
---

<Layout >
	<TamboInteractable client:only="react"/>
</Layout>
