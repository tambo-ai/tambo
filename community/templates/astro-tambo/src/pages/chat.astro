---
/**
 * Chat Page
 * 
 * This page renders the chat interface for the application.
 * It utilizes the `TamboChat` component which is rendered strictly on the client side
 * via `client:only="react"` to support real-time interactions and browser-specific features.
 */
import Layout from "@/layouts/Layout.astro";
import TamboChat from "@components/TamboChat"
---

<Layout >
	<TamboChat client:only="react"/>
</Layout>
