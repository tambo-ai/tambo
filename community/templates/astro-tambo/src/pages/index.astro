---
/**
 * Home Page
 * 
 * The main landing page of the application, serving as the entry point.
 * It renders the `Home` component wrapped in the main `Layout`.
 */
import Layout from "@/layouts/Layout.astro"
import Home from "@/components/TamboHome"
---

<Layout>
    <Home />
</Layout>