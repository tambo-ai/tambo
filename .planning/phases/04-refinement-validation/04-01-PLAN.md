---
phase: 04-refinement-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - AGENTS.md
  - package.json
autonomous: true
requirements:
  - TURBO-01
  - TURBO-02
  - DX-01

must_haves:
  truths:
    - "CONTRIBUTING.md documents hot reload workflows for both cloud and SDK development"
    - "AGENTS.md documents turbo watch usage and dev script options"
    - "Convenience scripts cover all common dev workflows"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Hot reload developer workflows documentation"
      contains: "turbo watch"
    - path: "AGENTS.md"
      provides: "Updated development commands and hot reload guidance"
      contains: "hot reload"
    - path: "package.json"
      provides: "Convenience dev scripts"
      contains: "dev:cloud"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "package.json"
      via: "npm run dev:* script references"
      pattern: "npm run dev:"
---

<objective>
Validate that Turborepo orchestration from Phases 1-3 satisfies TURBO-01, TURBO-02, and DX-01, then update developer documentation with hot reload workflows.

Purpose: Ensure developers know how to use the new hot reload setup and that convenience scripts are complete.
Output: Updated CONTRIBUTING.md and AGENTS.md with hot reload workflows; verified convenience scripts.
</objective>

<execution_context>
@/Users/lachlan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lachlan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-nestjs-hot-reload/03-01-SUMMARY.md
@turbo.json
@package.json
@CONTRIBUTING.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify orchestration and add missing convenience scripts</name>
  <files>package.json</files>
  <action>
Verify the following are already working from prior phases, and document findings:

1. TURBO-01 check: `turbo watch dev` (used by dev:cloud) re-runs package dev tasks when source files change. The `@tambo-ai-cloud/api#dev` task has `interruptible: true` and workspace `inputs` globs, so turbo watch restarts it when dependencies change. This is satisfied.

2. TURBO-02 check: `turbo watch dev --filter=@tambo-ai-cloud/web --filter=@tambo-ai-cloud/api` (the dev:cloud script) automatically builds dependencies first via `dependsOn: ["^build"]` on the dev task. This is satisfied.

3. DX-01 check: Review existing scripts in package.json:
   - dev (showcase + docs)
   - dev:sdk (react SDK watch + showcase)
   - dev:cloud (web + api with turbo watch)
   - dev:cloud:full (web + api + showcase + docs with turbo watch)
   - dev:local (script)
   - dev:web, dev:api, dev:docs (individual)

   If `dev:showcase` is missing as a standalone, add it: `"dev:showcase": "turbo dev --filter=@tambo-ai/showcase"`. Only add if not already present. Do NOT add scripts that duplicate existing ones.
   </action>
   <verify>Run `node -e "const p=require('./package.json'); Object.keys(p.scripts).filter(k=>k.startsWith('dev')).forEach(k=>console.log(k, '→', p.scripts[k]))"` from repo root to list all dev scripts.</verify>
   <done>All dev:\* convenience scripts exist for common workflows. TURBO-01, TURBO-02, DX-01 confirmed satisfied by existing config.</done>
   </task>

<task type="auto">
  <name>Task 2: Update CONTRIBUTING.md and AGENTS.md with hot reload workflows</name>
  <files>CONTRIBUTING.md, AGENTS.md</files>
  <action>
Update CONTRIBUTING.md to add/update a "Hot Reload Development" section near the existing development commands. Include:

1. **Cloud development** — `npm run dev:cloud` starts web + API with automatic restart when workspace packages change. Editing files in packages/core, packages/backend, or packages/db triggers API server restart automatically.

2. **SDK development** — `npm run dev:sdk` runs React SDK in watch mode alongside showcase. Edit react-sdk source and see changes reflected in showcase via HMR.

3. **Full stack** — `npm run dev:cloud:full` runs everything (web, API, showcase, docs) with hot reload.

4. Note that Next.js apps use `transpilePackages` to compile workspace TypeScript directly, so no manual rebuild of packages is needed for web/showcase/docs.

Update AGENTS.md section "### Commands" / "#### Development Commands" to:

- Add a brief note that `dev:cloud` uses `turbo watch` for automatic dependency rebuild
- Add `dev:showcase` if it was added in Task 1
- Add a "Hot Reload" subsection explaining the architecture: transpilePackages for Next.js apps, turbo watch with interruptible for NestJS API

Keep changes minimal and factual. Do not rewrite existing sections — add to them.
</action>
<verify>Grep CONTRIBUTING.md for "hot reload" or "turbo watch" to confirm new content exists. Grep AGENTS.md for "hot reload" or "turbo watch" to confirm.</verify>
<done>CONTRIBUTING.md has hot reload workflow documentation. AGENTS.md has updated dev commands and hot reload architecture notes.</done>
</task>

</tasks>

<verification>
1. `grep -i "hot reload\|turbo watch" CONTRIBUTING.md` returns matches
2. `grep -i "hot reload\|turbo watch" AGENTS.md` returns matches
3. All dev:* scripts in package.json cover: cloud, sdk, individual apps, full stack
4. `npm run check-types` passes
5. `npm run lint` passes
</verification>

<success_criteria>

- TURBO-01, TURBO-02, DX-01 confirmed satisfied (verified, not just assumed)
- CONTRIBUTING.md documents hot reload workflows for cloud and SDK development
- AGENTS.md updated with hot reload architecture and command notes
- No regressions in lint or type checks
  </success_criteria>

<output>
After completion, create `.planning/phases/04-refinement-validation/04-01-SUMMARY.md`
</output>
